extends Node2D


func _ready():
	var person = "you!"
	var greeting_Message = "What are we waiting for?"
	var godot_Name = "GODOT!"
	print ("Hello " + person)
	print (greeting_Message + "\n" + godot_Name)
